platform: linux

image_resource:
  type: docker-image
  source: {repository: busybox}

inputs:
- name: terraform-resource
- name: installed-terraform

outputs:
- name: prepared-terraform

run:
  path: ./terraform-resource/script/terraform_prepare.sh
